# Paths are fixed by the unit under ~/.local/state/mncs-commons. Keep this file
# mode 0600 even though it contains no credentials, so later operator-only
# settings do not widen it.
MNCS_COMMONS_DOMAIN=local
