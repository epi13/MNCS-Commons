# Public-node compatibility vectors

These vectors describe the experimental HTTP binding, not Commons record
identity. A future independent client may use them to check discovery, receipt,
error, cursor, and visibility handling without importing Python code.

The live loopback scenario is exercised by `scripts/validate_public_node.py`.
No vector claims that an Internet endpoint is currently deployed.
